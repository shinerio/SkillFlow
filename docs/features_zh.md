# SkillFlow — 完整功能参考手册

> 🌐 **中文** | [English](features.md)
>
> 本文档列举了 SkillFlow 的每一项功能、按钮、交互细节和 UX 设计。
> **每当功能增删改时，必须同步更新本文件。**

## 目录

1. [导航与外壳](#1-导航与外壳)
2. [我的技能（主面板）](#2-我的技能主面板)
3. [旧推送路由](#3-旧推送路由)
4. [旧拉取路由](#4-旧拉取路由)
5. [仓库收藏](#5-仓库收藏)
6. [云端备份](#6-云端备份)
7. [设置](#7-设置)
8. [技能卡片](#8-skill-卡片)
9. [技能悬浮提示](#9-skill-悬浮提示)
10. [公共对话框](#10-公共对话框)
11. [后端事件](#11-后端事件)
12. [应用更新对话框](#12-应用更新对话框)
13. [我的智能体](#13-我的智能体)
14. [我的提示词](#14-我的提示词)
15. [我的记忆](#15-我的记忆)

---

## 1. 导航与外壳

固定左侧边栏（宽 w-56）提供全局导航。

| 路由 | 图标 | 标签 |
|------|------|------|
| `/` | Package | 我的技能 |
| `/prompts` | FileText | 我的提示词 |
| `/memory` | Brain | 我的记忆 |
| `/tools` | Wrench | 我的智能体 |
| `/starred` | Star | 仓库收藏 |
| `/backup` | Cloud | 云备份 |
| `/settings` | Settings | 设置 |

- 当前路由：使用当前主题色的浅色块高亮，并配合克制的描边与层次阴影。
- 非当前路由：灰色文字，鼠标悬停时显示高亮。
- 侧边栏左上角：`SkillFlow` 标题文字左侧显示应用图标；图标会略高于标题文字以保证辨识度，并保持原始长宽比。
- 侧边栏右上角：**Languages** 语言快捷按钮，可在 **中文** 与 **English** 之间即时切换，并将偏好持久化到 `localStorage`。
- 侧边栏右上角：**调色盘** 主题快捷按钮，按 **Dark → Young → Light → Sport** 顺序即时轮换。
- 左侧主导航现在只保留四个核心工作入口：**我的技能**、**我的提示词**、**我的记忆**、**我的智能体**。
- 左侧底部工具区继续保留 **仓库收藏**、**云备份**、**设置** 和 **意见反馈**。
- 兼容旧链接：
  - `/sync/push` 会重定向到 `/`
  - `/sync/pull` 会重定向到 `/tools`
- 左下角 **意见反馈** 按钮：在默认浏览器中打开 GitHub 新建 Issue 页面。
- 窗口关闭行为：点击左上角关闭按钮时会直接结束当前 UI 进程，而不再只是隐藏主窗口。后台 `daemon` 会继续驻留在托盘/菜单栏，因此通过 `Show SkillFlow` 或再次启动应用都能重新打开一个新的窗口。
- 主页面刷新行为：路由切换时会按 `location.pathname` 重建页面子树，因此重新进入 **我的技能**、**我的智能体**、**我的提示词**、**仓库收藏** 等页面时，会再次拉取后端当前数据，而不需要在页面内手动刷新。
- 启动平滑策略：主界面准备好后，启动阶段的后台任务会错峰启动，而不是同时触发，因此首屏可交互的前几秒不容易和技能更新检测、收藏仓库刷新、应用更新检查、Git 启动拉取一起争抢资源。
- 进程驻留行为：当主窗口处于激活状态时，SkillFlow 同时运行后台 `daemon` 和前台 `ui` 两个进程；当窗口被隐藏到托盘/菜单栏后，只保留 `daemon` 常驻，`ui` 进程会直接退出并释放页面内存。
- 窗口重新激活行为：当窗口被隐藏到托盘/菜单栏时，UI 进程会直接退出，并释放当前页面状态对应的内存。再次显示 SkillFlow 时会重新冷启动一个新的 UI 进程，因此不会恢复上一次的路由页面树、滚动位置或进行中的交互。
- 窗口启动尺寸：启动时会优先恢复保存在本地 `config_local.json` 里的最近一次窗口尺寸；如果用户还没有手动调整过，则会根据当前显示器尺寸自适应计算初始窗口，使用更适合桌面的默认大小，并在屏幕可用空间内居中显示。
- macOS 托盘行为：后台 `daemon` 会在顶部状态栏保留黑白状态栏图标。关闭主窗口或点击 `Hide SkillFlow` 时都会退出 `ui` 进程，但状态栏菜单仍然保留，可执行 `Show SkillFlow`、`Hide SkillFlow` 和 `Quit SkillFlow`；当没有活动 UI 进程时，`Show SkillFlow` 会重新冷启动 UI 窗口。
- Windows 托盘行为：后台 `daemon` 会继续驻留在系统托管区并显示应用图标。关闭主窗口会退出 `ui` 进程；点击托盘菜单仍可通过 `Show SkillFlow` 重新拉起或聚焦 UI，也可通过 `Exit` 一并退出 `daemon` 和 `ui`。

---

## 2. 我的技能（主面板）

集中管理你的技能库。

### 工具栏

| 控件 | 操作 |
|------|------|
| **搜索框** | 加宽显示，实时按技能名过滤（不区分大小写）；窗口较窄时工具栏会自动换行，保证按钮完整可见 |
| **排序切换** | 两个按钮在 **A-Z** 与 **Z-A** 间切换，按技能名称首字母正序或逆序排列 |
| **更新**（RefreshCw） | 调用后端 `CheckUpdates()`；按规范化后的仓库来源 + 子路径聚合同一个 Git 技能，把已安装实例的 `SourceSHA` 与本地仓库缓存中同一子路径的最新 SHA 做比较，刷新可同步的 `LatestSHA` 与仅本地保存的 `LastCheckedAt`，为可更新卡片显示红点与“更新”操作，并在实例已是最新时清理过期更新标记。该按钮只检查，不会直接覆盖本地文件 |
| **手动推送**（ArrowUpFromLine） | 在当前页面内进入手动推送模式，不再跳转到独立页面 |
| **批量删除**（CheckSquare） | 切换多选模式 |
| **导入**（FolderOpen） | 打开系统文件夹选择器 → `ImportLocal(dir)` |
| **自动更新**（ToggleLeft / ToggleRight） | 继续占用原来远程安装按钮的位置，但现在改成明显的开关态按钮：关闭时显示低强调的“开自动更新”，开启后切成高强调的“关自动更新”，并通过左右切换图标直接区分当前状态。切换成功后会显示确认提示；开启后仍然会在启动时或手动刷新收藏仓库后，自动更新匹配的已安装 Git 技能 |

### 自动推送目标智能体

- 工具栏下方新增一条紧凑的单行选择区，把 **自动推送目标智能体** 标题和智能体按钮放在同一行，按钮样式与页面内的手动推送模式保持一致。
- 这组选择只保存在当前设备本地，并会在后续继续复用。
- 工具栏里的 **自动更新** 开关也只保存在当前设备本地，与自动推送目标智能体一起持久化。
- 这个按钮现在显示的是“下一步动作”而不是固定标题，并通过左右切换图标与高/低强调样式直接告诉你当前是开着还是关着。
- 勾选某个智能体后，会立即把当前 **我的技能** 里已有的技能补推送到该智能体，不需要等到下一次导入。
- 任何新添加到 **我的技能** 的技能，在成功导入库之后都会自动复制到这些选中的智能体中；覆盖本地导入、从智能体拉取、以及仓库收藏导入这几条入口。
- 如果云端恢复把库内技能同步到了当前设备，SkillFlow 会立刻对这些“新恢复”或“恢复后变成新版本”的技能执行自动推送，目标仍是当前设备已勾选的智能体。
- 本地导入的自动推送仍是非破坏性的：如果目标智能体的 `PushDir` 中已经存在同名技能，SkillFlow 会跳过该目标，不会覆盖已有内容。
- 之后取消勾选某个智能体时，也不会删除之前已经推送过去的技能；如需移除，仍需用户在 **我的智能体** 页面或智能体目录里手动删除。
- 点击 **手动推送** 后，页面会进入内联批量推送模式，而不是跳转到独立页面。
- 当前分类、搜索条件和排序结果共同决定本次可选技能范围。
- 手动推送模式下：
  - 技能卡片进入复选态
  - 工具栏下方的智能体选择区从“自动推送目标智能体”切换为“手动推送目标智能体”
  - 可对当前可见列表执行 **全选 / 取消全选**
  - 工具栏显示 **开始推送（n）** 和 **取消**
- 只有在至少选择 1 个目标智能体和 1 个技能时才允许开始推送。
- 推送前仍会执行缺失目录确认；推送冲突仍复用原有覆盖 / 跳过对话框，但流程现在发生在 **我的技能** 页面内。

### 多选模式（点击"批量删除"激活）

| 控件 | 操作 |
|------|------|
| **全选 / 取消全选** | 切换当前过滤后所有技能的选中状态 |
| **删除（n 个）**（Trash2，红色） | `DeleteSkills(ids)` — 未选择时置灰禁用 |
| **取消** | 退出多选模式并清空选择 |

### 分类侧边栏

- 列出所有分类；点击可过滤技能网格。
- **"全部" 按钮** — 显示所有分类的技能。
- **拖拽目标区域** — 将技能卡片拖到某个分类，即可移动至该分类。
- **拖拽高亮** — 拖动技能卡片悬停到分类上方时，目标分类会以更明显的高亮状态显示。
- **右键上下文菜单**（每个分类）：
  - **重命名** — 显示内联文字输入框；回车确认，Esc 取消；调用 `RenameCategory()`。（`Default` 分类不可用）
  - **删除** — 空分类会直接调用 `DeleteCategory()` 删除；若该分类下仍有技能，则弹出提示框要求先清空该分类后再删除。（`Default` 分类仍不可删除）
- **新建分类**（底部 Plus 图标） — 显示内联文字输入框；回车或失焦确认，Esc 取消；调用 `CreateCategory()`。

### 技能网格

- 布局：3 列，宽屏 4 列。
- 列表性能行为：进入页面时会优先读取本地派生快照来构建已安装技能列表；页面切换动画也改成更轻量的淡入淡出，以减少切页卡顿。
- 大列表动画降级：当 **我的技能** 当前可见卡片超过 18 张时，会自动关闭逐卡错峰入场动画，改为直接渲染，避免大列表时的掉帧。
- 卡片状态条溢出处理：状态徽标和已推送智能体图标现在使用固定截断与 `+N` 溢出显示，不再对每张卡片做运行时宽度测量。
- **空状态** — 显示"没有找到技能"及使用提示。
- **技能右键菜单** — 包含"移动到其他分类"项（仅显示非当前分类）、删除，以及适用时的更新操作。
- **拖拽操作** — 将技能卡片拖至侧边栏分类可移动；开始拖拽后，会有一个缩小版卡片跟随鼠标移动；命中左侧分类后，原卡片会收缩成一条细线，直到结束拖拽；将文件夹从操作系统文件管理器拖入窗口可直接导入。
- **窗口级拖拽遮罩** — 拖拽文件进入窗口时，显示半透明靛蓝色遮罩和"松开以导入技能"提示。
- **悬浮提示** — 鼠标悬停卡片 300 毫秒后出现（详见 [技能悬浮提示](#9-skill-悬浮提示)）。

### 技能更新流转

- 工具栏的 **更新** 只负责把已安装的 Git 技能与本地缓存仓库中的对应子路径做比较，并把卡片标记为“可更新”。
- 卡片上的 **更新** 才会真正从本地仓库缓存复制最新文件，覆盖 **我的技能** 中已安装的那一份副本，并同步刷新已经存在于各智能体 `PushDir` 中的同一技能副本；同时会对当前勾选了“自动推送目标智能体”的智能体执行强制同步（不存在则新建，存在则覆盖为最新版本）。
- 当主面板中的 **自动更新** 开关处于开启状态时，启动后的收藏仓库刷新，以及你在 **仓库收藏** 页面执行的单仓库刷新或全部刷新，都会自动触发同一套已安装技能更新流程。
- 自动更新只会为当前勾选了“自动推送目标智能体”的智能体创建或覆盖副本；未勾选的智能体会保留现有副本，并在该副本落后于 **我的技能** 时继续显示“可更新”。
- 卡片更新进行中时，该卡片会保持“更新”操作可见、禁止重复点击，并持续旋转 Refresh 图标直到请求结束。
- 主面板顶部还会显示临时状态提示条，明确告知当前正在更新、更新成功或更新失败，方便用户确认点击确实已触发操作。
- 基于缓存的检查会按应用内统一使用的 Git 逻辑主键聚合：规范化仓库来源 + 仓库子路径。
- 当多个已安装实例指向同一个逻辑 Git 技能时，缓存中的 SHA 只读取一次，但每个实例仍会用自己的 `SourceSHA` 决定自己是否 `updatable`。
- 如果对应的本地仓库缓存不存在或尚未刷新，这里不会退回到直接请求 GitHub API；需要先通过正常的仓库缓存同步链路更新该 clone。

```text
[主面板工具栏 更新]
         |
         v
    CheckUpdates()
         |
         v
比较本地 SourceSHA 与缓存仓库子路径的最新 SHA
         |
         +--> 没有更新  -> 卡片保持不变
         |
         +--> 有更新    -> 写入 LatestSHA -> 显示红点 / 更新按钮

[技能卡片 更新]
         |
         v
 UpdateSkill(skillID)
         |
         v
从本地缓存 clone 复制该技能对应的仓库子路径
         |
         v
覆盖“我的技能”中的安装目录
         |
         v
刷新各智能体 PushDir 中已存在的同一技能副本
         |
         v
SourceSHA = LatestSHA -> 清除更新标记
```

---

## 3. 旧推送路由

独立的“推送到智能体”页不再是主导航入口。

- `/sync/push` 作为兼容路由仍然保留。
- 打开后会重定向到 **我的技能**（`/`）。
- 原先的手动推送动作已经并入 **我的技能** 页内。

---

## 4. 旧拉取路由

独立的“从智能体拉取”页不再是主导航入口。

- `/sync/pull` 作为兼容路由仍然保留。
- 打开后会重定向到 **我的智能体**（`/tools`）。
- 原先的手动拉取动作已经并入 **我的智能体** 页内。

---

## 5. 仓库收藏

直接浏览并导入已关注 Git 仓库中的技能，无需先导入到库中。

- 仓库扫描会递归遍历整个克隆目录，因此 `plugins/<plugin>/skills/<name>` 这类嵌套技能目录也会被发现；`skill.md` 文件名按大小写不敏感匹配。
- 如果仓库根目录自身就包含 `SKILL.md` / `skill.md`，也会被视为一个技能候选项。
- 仓库递归扫描会受到 **设置 → 通用** 中 **远程仓库递归扫描深度** 控件的限制（默认 `5`，保存范围 `1-20`）。

### 视图模式

| 模式 | 图标 | 描述 |
|------|------|------|
| **文件夹视图** | Folder | 仓库卡片网格；点击卡片进入该仓库的技能列表 |
| **平铺视图** | LayoutGrid | 所有仓库的技能合并显示在一个网格中 |

### 工具栏（普通模式）

| 按钮 | 操作 |
|------|------|
| **搜索框** | 在平铺视图和仓库详情视图中显示；按名称过滤当前技能网格 |
| **排序切换** | 在平铺视图和仓库详情视图中显示；在 **A-Z** 与 **Z-A** 间切换 |
| **批量导入**（CheckSquare） | 仅在有技能网格可见时进入多选模式 |
| **全部更新**（RefreshCw） | `UpdateAllStarredRepos()` — 并行克隆/拉取所有仓库；更新中图标旋转 |
| **添加仓库**（Plus，靛蓝色） | 打开"添加仓库"对话框 |

### 工具栏（多选模式）

| 按钮 | 操作 |
|------|------|
| **全选 / 取消全选** | 切换所有可见技能 |
| **推送到智能体（n）** | 打开推送到智能体对话框（遗留流程，仍可在仓库收藏中使用） |
| **导入到我的技能（n）** | 打开导入对话框 |
| **取消** | 退出多选模式 |

### 仓库卡片（文件夹视图）

- 点击打开该仓库的技能列表。
- 卡片头部会额外展示仓库主机（如 `github.com`）和紧凑的技能数量，便于在钻取前快速判断仓库来源与规模。
- **在浏览器中打开**（ExternalLink 图标） — 在默认浏览器中打开仓库 URL。
- **更新**（RefreshCw 图标） — `UpdateStarredRepo(url)` — 拉取最新提交。
- **删除**（Trash2 图标，悬停变红） — 从收藏列表中移除。
- 卡片下方显示最后同步时间及同步错误信息。

### 内置起始仓库初始化

- 仅在 `star_repos.json` 尚不存在时（设备/配置的首次初始化）才会注入内置起始仓库。
- 当前内置起始仓库：
  - `https://github.com/anthropics/skills.git`
  - `https://github.com/ComposioHQ/awesome-claude-skills.git`
  - `https://github.com/affaan-m/everything-claude-code.git`
- 一旦用户删除内置仓库，持久化列表即为唯一事实来源；后续启动不会再自动把该仓库加回。

### 仓库详情视图（钻取）

- 顶部面包屑返回按钮（ChevronLeft）回到仓库列表。
- 技能网格与平铺视图的选择/导入操作相同。
- 当仓库内技能仍在加载时，顶部计数和内容区都会显示 **加载中...**，而不会短暂显示 `0 个技能` 或“没有找到技能”空状态。
- 这个页面的仓库技能卡片只显示“已导入 / 已推送到智能体”两类状态。
- “已导入”徽章按规范化仓库来源 + 子路径解析，因此不同仓库里的同名技能不会再互相误判。
- `pushed` 状态会以智能体品牌图标显示，并支持悬浮查看完整智能体列表。

### 仓库同步 与 已安装技能更新的区别

- 仓库卡片上的 **更新** 和工具栏的 **全部更新**，更新的是该收藏仓库在本地的缓存克隆。
- 这样会让 **仓库收藏** 页面看到最新仓库内容，便于继续浏览或导入新的技能文件。
- 如果云端恢复把新的收藏仓库同步到了当前设备，SkillFlow 会立即在本地克隆这些新恢复下来的仓库，这样搜索、导入和直接推送到智能体都不需要再手动点一次更新。
- 它 **不会** 覆盖 **我的技能** 中已经安装好的那一份技能。
- 如果某个技能已经导入到库中，要更新那份已安装副本，仍然要回到 **我的技能（主面板）** 使用卡片上的 **更新**。

```text
                    跨页面更新流转图

[仓库卡片 更新]                           [我的技能 卡片 更新]
        |                                           |
        v                                           v
UpdateStarredRepo(url)                       UpdateSkill(skillID)
        |                                           |
        v                                           v
刷新本地仓库缓存克隆                         覆盖已安装的库内副本
        |                                           |
        v                                           v
刷新仓库收藏中的技能列表                  清除我的技能中的更新标记
        |
        +--> 让后续导入拿到更新后的内容
        x--> 不会直接修改“我的技能”文件
```

### 添加仓库对话框

- URL 输入框（HTTPS 或 SSH 格式）；回车触发添加。
- **"添加"** 按钮 — `AddStarredRepo(url)`。
- 若仓库需要 HTTP 认证，自动弹出 **HTTP 认证对话框**。
- 若 SSH 认证失败，弹出 **SSH 认证错误对话框**，说明所需配置。
- 显示克隆进度状态（"克隆中…"）。

### HTTP 认证对话框

- 用户名 + 密码输入框（密码掩码）；密码框按回车确认。
- **"确认"** — 重试调用 `AddStarredRepoWithCredentials(url, user, pass)`。
- **"取消"** — 中止。
- 凭据错误时显示错误提示。

### SSH 认证错误对话框

- 说明 SSH 密钥配置检查清单：
  - 是否已通过 `ssh-keygen` 生成密钥
  - 公钥是否已添加到 GitHub / GitLab
  - SSH Agent 是否正在运行（`ssh-add`）
  - 建议改用 HTTPS 协议克隆
- **"关闭"** 按钮。

### 导入对话框（到我的技能）

- 分类选择下拉菜单。
- **"导入 n 个"** — `ImportStarSkills(paths, repoURL, category)`。
- **"取消"**。

### 推送到智能体对话框

- 说明：将技能直接复制到智能体目录，无需先导入到“我的技能”。
- 以复选框列表展示所有已启用智能体，右侧显示推送目录路径。
- 无智能体时显示提示信息。
- **"推送到 n 个智能体"** 按钮。
- 若存在冲突，后续对话框会列出被跳过的精确 `skill → agent` 配对；**全部覆盖** 只会覆盖这些列出的冲突配对。
- **"取消"**。

### 目录缺失确认

与当前 **我的技能** 页内手动推送流程一致：在创建缺失推送目录前弹框确认。

### 推送冲突对话框

当技能在目标智能体目录中已存在时：

- 列出所有冲突的技能名称。
- **"覆盖全部"**（琥珀色） — `PushStarSkillsToAgentsForce()`。
- **"跳过冲突"** — 跳过已存在的，仅推送新的。

---

## 6. 云端备份

将技能库镜像备份至云端。支持两种后端：**对象存储**（阿里云 OSS、AWS S3、Azure Blob Storage、Google Cloud Storage、腾讯云 COS、华为云 OBS）和 **Git 仓库**。

### 状态提示

- **云备份未启用提示**（黄色横幅） — 未配置时显示，引导前往设置。

### 操作

| 按钮 | 对象存储标签 | Git 标签 |
|------|------------|---------|
| **立即备份**（Upload 图标） | 立即备份 | 立即备份 |
| **恢复 / 拉取**（Download 图标） | 从云端恢复 | 拉取远端 |
| **刷新**（RefreshCw） | 重新加载最近一次备份的变更结果 | 同左 |

- 云备份未配置时，两个操作按钮均禁用。
- **"备份完成 / Git 同步完成"**（绿色）/ **"备份/同步失败"**（红色）状态消息。

### 本次备份变更列表

- 每次备份或恢复成功后，页面只显示本次操作涉及的文件，不再展示远端全量文件集合。
- 备份页会显示当前应用会话中最近一次成功备份/恢复的**上次同步完成时间**。
- 对象存储与 Git 共用同一种结果列表，按 `新增`、`修改`、`删除` 三种动作展示。
- 已删除文件显示删除标记，不显示文件大小。
- 点击刷新会重新读取当前应用会话里最近一次备份结果。
- 可滚动容器（最大高度限制）。
- **统一备份范围（所有服务商）** — 备份始终以 `AppDataDir` 作为 Git/cloud 工作根目录。会参与同步的内容固定位于其中的 `skills/`、`meta/`、`prompts/` 等目录；本地专属的 `cache/`、`runtime/`、`logs/`、`meta_local/`、`.git/`、`config_local.json`、`star_repos_local.json` 会被排除。收藏仓库的 clone cache 位于 `repoCacheDir`，也不会进入备份范围。
- **对象存储自定义前缀** — 对象存储服务商允许用户填写父级 `remotePath`；SkillFlow 最终总是写入 `<存储桶>/<remotePath>/skillflow/`（若父级路径为空，则为 `<存储桶>/skillflow/`）。
- **云服务商独立配置档案** — 每个云服务商都会保留自己独立的存储桶、路径和凭据配置；在设置中切换服务商时，会恢复该服务商上次保存的表单值，而不会覆盖其他服务商的配置。
- **同步路径可移植** — `meta/*.json` 等同步元数据中的本地路径会以 `AppDataDir` 下的正斜杠相对路径保存，确保 macOS 与 Windows 间恢复后仍能正确定位。
- **技能易变检查时间仅本地保存** — 变化频繁的 `LastCheckedAt` 会写入本地 `meta_local/*.local.json`，不参与 git/云端同步，降低多机并行时的冲突概率。
- **收藏仓库同步运行态仅本地保存** — 每个仓库的 `lastSync` 与 `syncError` 会写入本地 `star_repos_local.json`，避免某台设备后台同步导致其他设备的同步元数据频繁抖动与冲突。
- **本地路径配置隔离** — `config_local.json` 保存机器相关的文件系统路径与代理设置，例如 `repoCacheDir`、智能体 `ScanDirs` / `PushDir` / `MemoryPath` / `RulesDir` 以及代理配置；该文件不参与备份和 git 同步。
- **其他设备本地状态仅本地保存** — 自动推送目标智能体、开机自启注册状态、最近一次窗口尺寸等设备级状态会继续保存在 `config_local.json` 中，因此某一台机器执行恢复时不会覆盖另一台机器的本地使用习惯。
- **云端敏感凭据仅本地保存** — Access Key ID、Secret Key、访问令牌等敏感云凭据只会写入 `config_local.json` 中按服务商分组的本地配置；可同步的 `config.json` 仅保留云服务商、存储桶、远端路径、Endpoint、仓库地址、分支等非敏感配置。
- **Git 备份兼容处理** — 当 Git 备份以父目录作为工作树时，SkillFlow 会自动迁移旧的 `skills/.git` 嵌套元数据，避免真实技能文件被当作嵌套仓库而无法跟踪。
- **恢复后的本机补偿动作** — 云端恢复成功后，当前设备上新恢复出来的库内技能会自动推送到已勾选的自动推送目标智能体；新恢复出来的收藏仓库也会立刻在本地完成克隆。

### 服务商覆盖范围

- 对象存储除阿里云 OSS、腾讯云 COS、华为云 OBS 外，还支持 AWS S3（bucket + region）、Azure Blob Storage（container + account name + 可选 service URL）以及 Google Cloud Storage（bucket + service-account JSON 或本地 key file path）。
- 可同步的连接字段现在除既有的 endpoint / repo URL / branch 外，还包括 `region`、`account_name`、`service_url`。Account key、service-account JSON 等敏感值仍只保存在本地 `config_local.json`。

### 自动备份

以下操作完成后（在云备份已启用时）自动触发：

- 删除技能 / 批量删除
- 新建 / 编辑 / 删除提示词
- 手动导入
- 从智能体拉取
- 更新技能
- 从收藏仓库导入

通过 Wails 事件系统向前端推送进度事件：`backup.started`、`backup.progress`、`backup.completed`、`backup.failed`。

### Git 同步（仅 git 服务商）

选择 **git** 服务商后：

- **仓库自举初始化** — 若技能目录还不是 Git 仓库，SkillFlow 会自动执行初始化，并按配置仓库地址设置 `origin`。
- **远端绑定自动修复** — 若 `origin` 缺失或地址变更，拉取/推送前会自动补齐或更新。
- **启动时自动拉取** — 每次 App 启动时，自动对 Git 备份根目录执行 `git pull`，拉取远端最新内容。
- **启动后台任务错峰** — 启动拉取依然会自动执行，但不会再和其他所有启动检查在同一时间爆发；SkillFlow 会在界面可交互后分批启动这些后台任务。
- **远端分支缺失容错** — 首次配置时若远端分支尚未创建，启动拉取会跳过而非直接报错。
- **变更后自动推送** — 与对象存储相同的触发时机；执行 `git add -A && git commit && git push`。
- **排除路径自动清理** — 每次 Git 备份推送前，像 `cache/`、`runtime/` 这样的本地专属排除目录，如果曾被旧版本错误跟踪，会自动从 Git 索引中移除。
- **定时自动同步** — 由"定时自动同步间隔"设置控制（单位：分钟；0 表示禁用）。后台定时器按配置间隔调用 `autoBackup()`。
- **手动操作冲突检测** — 点击 **立即备份** 与 **拉取远端** 时都会检测 Git 冲突/分叉，若需要用户决策会触发 `git.conflict` 事件。
- **冲突解决对话框** — 若 `git pull` 或 `git push` 检测到冲突或历史分叉，弹出模态框：
  - 对话框会在可用时展示冲突相关文件列表。
  - **"以本地为准"** — 中止合并，将本地状态强制推送到远端。调用 `ResolveGitConflict(true)`。
  - **"以远端为准"** — 中止合并，将本地重置为 `origin/<分支>`。调用 `ResolveGitConflict(false)`。
  - **"手动解决"** — 在系统文件管理器中打开 Git 备份根目录，方便用户直接检查并处理冲突文件。
  - “以本地为准”和“以远端为准”成功后都会从磁盘重载应用状态（skills/meta/config），并发送 `git.sync.completed` 事件。
- **拉取后即时刷新** — 启动自动拉取或手动拉取成功后，会立刻重载磁盘状态，使 `meta/` 与配置变更立即生效。
- 若冲突发生在启动阶段（前端尚未加载），将以待处理标志存储，并在备份页面挂载时通过 `GetGitConflictPending()` 弹出对话框。

---

## 7. 设置

配置面板，当前标签顺序为：智能体路径、云存储、代理、通用。

设置页内容会随窗口宽度自适应展开，并在桌面端使用更宽松的最大内容宽度，而不是固定停留在较窄的一列。

### 智能体路径标签页

为每个内置或自定义智能体提供以下配置：

| 控件 | 说明 |
|------|------|
| **启用开关** | 在整个应用中启用或禁用该智能体 |
| **Skill 路径分组** | 独立展示与 Skill 分发相关的路径设置 |
| **推送路径** | 推送时复制技能的目标目录（唯一）；支持直接输入文字和点击文件夹图标（FolderOpen）打开系统文件管理器选择，并会定位到当前路径或最近存在的父目录 |
| **扫描路径** | 拉取时搜索的目录（可多个）；每条记录有文件夹选择按钮和删除按钮；通过"输入框 + 文件夹选择 + 添加"新增，选择器会重新定位到当前路径或最近存在的父目录。内置智能体的扫描路径可以和推送路径不同；Copilot 默认把推送路径保持在 `~/.copilot/skills`，并单独扫描其他共享来源 |
| **记忆路径分组** | 独立展示与记忆同步相关的路径设置 |
| **记忆文件** | 智能体主记忆文件路径 |
| **规则目录** | 智能体规则目录路径。对于没有官方规则目录概念的内置智能体（如 Copilot），该字段默认留空 |
| **删除智能体**（仅自定义智能体） | 移除该自定义智能体条目 |

**添加自定义智能体** 入口（虚线边框区域）：

- 点击后打开居中弹窗，而不是页内内联表单。
- 弹窗收集：
  - 智能体名称
  - 初始 Skill 路径（`pushDir`）
  - 记忆文件（`memoryPath`）
  - 规则目录（`rulesDir`）
- 创建时会自动把同一条 Skill 路径写入初始 `scanDirs`。
- 后续如需更多扫描路径，仍在该智能体卡片的 **Skill 路径** 分组里继续添加。
- 弹窗只更新当前设置页内存草稿；真正落盘仍通过页面底部的 **保存设置**。

### 云存储标签页

| 控件 | 说明 |
|------|------|
| **云服务商按钮** | 以可自动换行的卡片网格展示。**Git 仓库** 会始终固定在第一位，其余服务商保持后端返回顺序；切换时会自动恢复该服务商各自保存的存储桶 / 路径 / 凭据草稿 |
| **存储桶名称** | 对象存储桶名称（选择 git 服务商时隐藏） |
| **远程路径** | 对象存储的父级路径（可选）。用户只需填写父目录，SkillFlow 会自动在末尾追加 `/skillflow/` 作为最终备份前缀 |
| **最终备份路径预览** | 实时渲染对象存储的最终目标路径，显示为 `<存储桶>/<remotePath>/skillflow/`，方便用户在保存前确认真实备份位置 |
| **凭据字段** | 根据 `RequiredCredentials()` 动态渲染 — 文本或密码输入框，因服务商而异。Endpoint / 仓库地址 / 分支等非敏感连接信息保存在 `config.json`，Access Key / Secret / Token 等敏感凭据仅保存在 `config_local.json`。Git 字段：仓库地址、分支、用户名、访问令牌 |
| **输入规范化** | 阿里云 OSS 和华为云 OBS 的存储桶字段既可填写纯桶名，也可填写常见的完整桶域名/URL，保存时会自动提取桶名。腾讯云 COS 与其他对象存储一样使用独立的存储桶 + Endpoint 模型。存储桶始终来自单独的存储桶输入框，而 Endpoint 字段既可填写纯 Endpoint host，也可填写完整桶域名/URL，并会按用户输入形式保留用于展示 |
| **附加服务商细节** | AWS S3 在保存前会去除 region 字段首尾空白。Azure Blob Storage 使用存储桶字段作为 container 名称，将 account name 和可选 service URL 作为可同步字段保存，并在 service URL 为空时默认使用 `https://<account>.blob.core.windows.net/`。Google Cloud Storage 支持填写内联 service-account JSON 或本地 key file path，并且该凭据始终只保存在本地 `config_local.json`。 |
| **定时自动同步间隔** | 数字输入框（分钟）；0 表示仅在变更后同步；正数启动后台定时器 |
| **启用自动云备份开关** | 控制每次写操作后是否自动触发备份，同时控制定时器 |

### 通用标签页

| 控件 | 说明 |
|------|------|
| **语言** | 两个按钮 **中文** 与 **English** 可立即切换整个前端界面语言；与侧边栏顶部的 **Languages** 快捷按钮共享同一状态，并持久化到 `localStorage` |
| **外观主题** | 以四张预览卡片展示 **Young**（默认，由原浅色主题演化而来，改成更柔和的纸感浅蓝）、**Dark**（重做为石墨灰底色 + 雾蓝点缀）、**Light**（参考 Messor 的低饱和灰白 + 苹果蓝强调色）和 **Sport**（薄荷色壳层 + 草场绿强调色，整体更有运动感）；持久化到 `localStorage`；切换立即生效，无需重启；旧版存储的 `Light` 偏好会自动迁移到 `Young` |
| **应用数据目录** | 当前设备上已安装技能、提示词、元数据以及备份工作状态的固定根目录；只读展示，并提供一键打开 |
| **仓库缓存目录** | 收藏仓库 clone cache 的本地专属根路径；支持直接输入和文件夹选择按钮，选择器会定位到当前路径或最近存在的父目录；默认值为 `<AppDataDir>/cache/repos` |
| **技能递归扫描深度** | 扫描本地智能体目录与仓库收藏时允许的最大递归深度；默认 `5`；保存时会自动限制在 `1-20` 之间，以避免异常嵌套目录 |
| **默认分类** | 固定系统兜底分类 `Default`（只读），用于未指定分类的拉取/导入 |
| **日志级别按钮** | 在 `debug`、`info`、`error` 间切换运行日志级别（默认：`error`）；保存设置后生效 |
| **开机自启开关** | 控制是否向操作系统注册登录启动项，使 SkillFlow 在当前设备登录系统后自动启动；仅保存到本地 `config_local.json`。应用会在启动时和设置保存后执行 reconcile：如果目标状态为关闭且系统里本来就没有登录启动项，会视为无操作成功；如果目标状态为开启，则会把当前可执行文件路径重新写回注册项，保证 macOS 和 Windows 上移动或更新应用后的开机自启仍然有效 |
| **打开日志目录** | 一键在系统文件管理器中打开本地日志目录；若目标不存在，则回退到最近存在的父目录 |

日志文件位于应用日志目录，滚动规则如下：
- 最多仅保留 **2 个文件**：`skillflow.log` 和 `skillflow.log.1`。
- 单文件最大 **1MB**。
- 当 `skillflow.log` 达到上限后会滚动，并覆盖较旧的备份日志文件。

### 代理标签页

所有远程操作（仓库扫描、仓库缓存同步）的代理设置：

| 模式 | 说明 |
|------|------|
| **不使用代理** | 直接连接 |
| **使用系统代理** | 读取 `HTTP_PROXY` / `HTTPS_PROXY` 环境变量 |
| **手动配置** | 自定义代理地址（http://、https://、socks5://） |

选择"手动配置"后出现 URL 输入框，并显示支持的格式说明。代理设置保存在 `config_local.json` 中，因此重启后会恢复，且不会参与备份或 git 同步。

- 新增独立的 **测试连接** 区块，用户无需先保存设置就可以验证当前代理是否可用。
- 测试目标 URL 默认填充为 `https://github.com`，也可以改成任意 `http` / `https` 地址。
- 测试时优先使用当前页面内存里的代理表单状态，因此未保存的修改也会立即生效；如果用户没有改动，则该状态自然等于已保存配置。
- 每次测试都强制 **5 秒超时**，并在页面内直接显示目标地址、耗时，以及最终拿到的 HTTP 状态或传输层错误信息。
- 只要成功拿到 HTTP 响应，就视为代理链路连通；因此像 `301`、`403` 这类非 `2xx` 状态也会算作测试成功。

### 保存按钮

- **"保存设置"** — `SaveConfig(cfg)`；保存中时禁用。
- **快捷键** — 在设置页打开时，Windows / Linux 按 `Ctrl+S`、macOS 按 `Cmd+S` 会触发与“保存设置”完全相同的保存动作，同时阻止默认的页面保存快捷键行为。

---

## 8. 技能卡片

在“我的技能”网格和同步页面中使用的可复用卡片组件。

### 两种变体

**主面板卡片**（`SkillCard`）：

| 元素 | 说明 |
|------|------|
| **状态条** | 将来源徽章与其他并存状态（例如可更新 + 已推送智能体图标）统一收纳在卡片头部的紧凑区域中；空间足够时优先保持单行，卡片过窄时会自动换行，而不是把状态截断或隐藏 |
| **技能名称** | 最多显示两行；保留操作按钮所需间距 |
| **打开目录按钮**（FolderOpenDot，右上角） | `OpenPath(skill.path)` — 在系统文件管理器中打开目录；仅悬停时可见 |
| **选择复选框**（左上角） | 仅多选模式下可见 |
| **悬停操作**（右下角） | 更新（有新版时）· 复制 · 删除 — 默认悬停前隐藏，但当前卡片正在更新时会保持“更新”操作可见 |
| **更新操作反馈** | 悬停时会增加抬升与高亮动画；点击后会切换成禁用的转圈状态，直到更新完成 |
| **复制按钮** | 读取 `skill.md` 内容，复制到剪贴板，显示"已复制 ✓"持续 2 秒 |
| **拖拽手柄** | 普通模式下可拖拽；拖拽 `skillId` 到侧边栏分类可移动 |
| **右键上下文菜单** | 更新（有新版时）· 移动到 [分类]（每个其他分类一项）· 删除（红色） |

**同步页卡片**（`SyncSkillCard`）：

| 元素 | 说明 |
|------|------|
| **状态条** | 同时显示来源徽章与当前页面允许展示的状态（例如“已导入 / 可更新 / 已推送智能体”），并保持在同一块紧凑头部区域内；像“已导入”和“已推送智能体”这类并存状态在放得下时会优先同排展示，放不下时再自动换到下一行，确保完整可见 |
| **已推送智能体指示器** | 用小尺寸智能体品牌图标显示哪些智能体的 `PushDir` 已包含该逻辑 skill；不再额外显示箭头前缀，过多时折叠为紧凑计数徽章，并可悬浮查看完整列表 |
| **技能名称** | 最多显示两行 |
| **副标题** | 分类名或仓库名 |
| **复制按钮**（悬停） | 剪贴板操作同主面板卡片 |
| **打开目录按钮**（悬停） | 同主面板卡片 |
| **选择复选框**（右下角） | `showSelection = true` 时显示 |

### 统一状态语义

| 状态 | 含义 |
|------|------|
| **installed** | 该逻辑技能已经在 **我的技能** 中存在至少一个已安装实例 |
| **imported** | 外部来源页面对 **installed** 的文案别名；在 GitHub / 仓库收藏 / 智能体页面中表示“已经在我的技能里” |
| **pushed** | 该逻辑技能已经存在于某个智能体配置的 **PushDir** 中 |
| **pushedAgents** | 哪些智能体的 **PushDir** 当前包含该逻辑技能；卡片用它来渲染智能体图标列表 |
| **seenInAgentScan** | 该逻辑技能已在某个智能体配置的 **ScanDirs** 中被扫描到；表示智能体里某处已经有它，但不一定是 SkillFlow 推过去的。当前它主要用于分组和关联，不再作为卡片 badge 单独展示。 |
| **updatable** | 某个已安装的 Git 技能，其远端最新 SHA 比本地 `SourceSHA` 更新 |

这些状态由后端基于统一逻辑主键模型计算；前端页面不再独立根据 `Name` 或 `Path` 推断。

```text
                 统一状态语义示意图

[GitHub 候选项] [仓库收藏技能] [智能体扫描候选项]
        \              |                /
         \             |               /
          +------ 逻辑上是同一个技能 ------+
                         |
                         v
                  [我的技能 实例]
                   installed = true
             外部页面显示 imported = true
                         |
                         +---- 复制到智能体 PushDir --> pushed = true
                         |
                         +---- 远端有更高 SHA -------> updatable = true

[智能体 ScanDirs] --- 扫描到同一逻辑技能 ---> seenInAgentScan = true
```

---

## 9. 技能悬浮提示

鼠标悬停任意主面板技能卡片 300 毫秒后出现的浮动信息面板。

### 定位规则

- 固定定位，宽 300 px，最大高 400 px。
- 优先显示在卡片右侧；靠近右边界时切换到左侧。
- 靠近底部时向上偏移。

### 内容

| 区域 | 显示字段 |
|------|---------|
| **标题区** | 图标（GitHub / 文件夹）· 技能名称 · 来源徽章 · 分类 |
| **描述** | 从 `skill.md` YAML 前置元数据解析；无描述时显示"暂无描述"；加载中显示"加载中…" |
| **前置元数据字段** | `argument_hint`（Tag 图标）· `allowed_tools`（Wrench 图标）· `context`（GitBranch 图标） |
| **元数据区** | 仓库 URL（截断，可点击）· 当前 SHA · 可更新 SHA（琥珀色）· 安装日期 · 更新日期 |

---

## 10. 公共对话框

### 10.1 冲突对话框

推送或拉取时，目标位置已存在同名技能，逐条弹出。

- 显示："\[技能名称\] 已存在，如何处理？"
- **"跳过"** — 保留现有文件，处理下一个冲突。
- **"覆盖"** — 调用 `*Force` 变体，但只覆盖当前这一个冲突的 skill-target 配对。
- 冲突队列清空后自动关闭。

### 10.2 目录缺失对话框

推送前，目标目录不存在时弹出。

- 列出每个受影响的智能体名称及完整目录路径。
- **"创建并推送"** — 自动创建目录后继续。
- **"取消"** — 中止。

---

## 11. 后端事件

Go 后端通过 Wails runtime 向前端推送的事件：

| 事件 | 触发时机 | 载荷 |
|------|---------|------|
| `backup.started` | 自动备份开始 | — |
| `backup.progress` | 每上传一个文件 | `{ currentFile: string }` |
| `backup.completed` | 备份完成 | `{ files: Array<{ path, size, action }> }` |
| `backup.failed` | 备份出错 | — |
| `update.available` | 发现技能的缓存仓库中有新提交 | `{ skillID, skillName, currentSHA, latestSHA }` |
| `star.sync.progress` | 单个仓库同步完成 | `{ repoURL, repoName, syncError }` |
| `star.sync.done` | 所有仓库同步完成 | — |
| `git.sync.started` | Git 拉取/推送开始 | — |
| `git.sync.completed` | Git 同步成功 | 由备份 / 恢复 / 冲突解决触发时为 `{ files: Array<{ path, size, action }> }` |
| `git.sync.failed` | Git 同步出错 | — |
| `git.conflict` | 检测到 Git 合并冲突 | `{ message: string, files?: string[] }` |

主面板监听 `update.available` 事件，实时在对应技能卡片上标记红色更新红点。
备份页监听所有 `git.*` 事件，并在收到 `git.conflict` 时弹出冲突解决对话框。
`App.tsx` 监听全部三个 `app.update.*` 事件，解析发出的 `AppUpdateInfo` payload，并驱动更新对话框的状态机。

---

## 12. 应用更新对话框

检测到新版本时弹出的居中模态对话框，同时适用于启动时自动检测和设置页手动检测两个入口。对话框由四态状态机驱动：

| 状态 | 触发条件 | 对话框内容 |
|------|---------|----------|
| `available` | 收到 `app.update.available` 事件 | 版本号 + 更新说明 + 平台对应操作按钮 |
| `downloading` | 用户点击"下载并自动重启更新"（仅 Windows） | 旋转图标 + 下载进度提示 |
| `ready_to_restart` | 收到 `app.update.download.done` 事件 | 完成提示 + "立即重启" / "稍后重启"按钮 |
| `download_failed` | 收到 `app.update.download.fail` 事件 | 错误提示 + "前往下载页"按钮 |

### 平台行为

启动检测与手动检测均使用同一模态对话框。

- **Windows** — `available` 状态提供三个选项：
  1. **下载并自动重启更新** — 后台下载新 exe，完成后提示重启。
  2. **前往 Release 页面手动下载** — 在系统浏览器打开 GitHub Releases 页面。
  3. **跳过此版本（下次启动不再提示）** — 记住跳过的版本；启动检测不再对该版本弹窗，手动检测不受影响，仍正常弹出。
- **macOS** — `available` 状态提供两个选项（暂不支持自动下载）：
  1. **前往 Release 页面手动下载** — 在系统浏览器打开 GitHub Releases 页面。
  2. **跳过此版本（下次启动不再提示）** — 与 Windows 行为一致。
- 两个平台都使用同一份 `AppUpdateInfo` payload 渲染当前版本、最新版本和 Release 页面入口。

### 跳过版本机制

- 跳过的版本 tag 存储在 `AppConfig.SkippedUpdateVersion`，并写入共享的 `config.json`，因此重启应用后仍会生效。
- 启动检测时，若 `latestVersion == skippedUpdateVersion`，则**不**发布 `app.update.available` 事件，不弹出对话框。
- 设置页手动点击"检测更新"时，`CheckAppUpdateAndNotify` 始终发布事件，不受跳过版本影响 — 手动检测总是正常弹出对话框。
- 点击"跳过此版本"时调用 `SetSkippedUpdateVersion(latestVersion)`。

### 手动检测按钮（设置页）

设置页标题栏右上角提供 **"检测更新"** 按钮：

- 按钮旁显示当前应用版本号（`vX.Y.Z`）。
- 点击 → 调用 `CheckAppUpdateAndNotify()`；检测中按钮显示旋转图标。
- 若发现新版本，更新对话框通过 `app.update.available` 事件自动弹出。
- 若已是最新版本，内联显示"已是最新版本 (vX.Y.Z)"。
- 出错时内联显示"检测失败: …"。

### 操作说明

| 控件 | 操作 |
|------|------|
| **下载并自动重启更新**（Windows，`available` 状态） | `DownloadAppUpdate(downloadUrl)` — 启动后台异步下载 |
| **前往 Release 页面手动下载**（`available` 状态） | `OpenURL(releaseUrl)` — 在浏览器打开发布页；关闭对话框 |
| **跳过此版本**（`available` 状态） | `SetSkippedUpdateVersion(latestVersion)` — 持久化跳过版本；关闭对话框 |
| **立即重启**（`ready_to_restart` 状态） | `ApplyAppUpdate()` — 写入批处理脚本并退出；脚本完成 exe 替换后重启 |
| **稍后重启**（`ready_to_restart` 状态） | 关闭对话框，不立即重启 |
| **前往下载页**（`download_failed` 状态） | `OpenURL(releaseUrl)` — 在浏览器打开发布页；关闭对话框 |
| **×**（除 `downloading` 外所有状态） | 关闭对话框（本次会话内不再提示） |

### 后端方法

| 方法 | 说明 |
|------|------|
| `GetAppVersion()` | 返回当前版本字符串（CI 通过 `-ldflags` 注入；本地开发为 `"dev"`） |
| `CheckAppUpdate()` | 查询 GitHub Releases API；返回含平台匹配下载链接的 `AppUpdateInfo` |
| `CheckAppUpdateAndNotify()` | 调用 `CheckAppUpdate()` 并在有更新时发布 `EventAppUpdateAvailable`；始终通知，不受跳过版本影响 |
| `GetSkippedUpdateVersion()` | 返回 config 中存储的用户跳过版本 tag |
| `SetSkippedUpdateVersion(version)` | 将跳过版本 tag 持久化到 config；传空字符串可清除 |
| `DownloadAppUpdate(url)` | 异步下载新 exe 到临时目录；完成后发布 `app.update.download.done` 或 `app.update.download.fail` 事件 |
| `ApplyAppUpdate()` | 仅 Windows — 写入批处理脚本实现退出后 exe 替换，然后调用 `os.Exit(0)` |

### 版本注入（CI）

GitHub Actions 在编译时通过 ldflags 注入 tag 名：
```
wails build -ldflags "-X main.Version=${{ github.ref_name }}"
```
当 `Version == "dev"` 时（本地开发），启动时的更新检测会跳过。

---

## 13. 我的智能体

浏览每个已启用智能体当前目录中的技能。

### 布局

- 左侧边栏列出所有已启用智能体。
- 右侧主区域包含一个共享工具栏，以及顶部二段切换的技能 / 记忆视图。
- 页面首次进入时默认落在技能面板。
- 中文模式下，主工作流不再混用 `Skills | Memory` 这类英文标签。
- 切换到其他智能体时会保留当前面板选择，不会强制跳回技能面板。

### 工具栏

| 控件 | 说明 |
|------|------|
| **技能 / 记忆切换** | 在技能管理视图和记忆预览视图之间切换右侧主内容 |
| **搜索框** | 只实时过滤当前激活的面板 |
| **排序切换** | 保持在同一个工具栏里；在技能面板中作用于推送路径和扫描路径卡片，在记忆面板中作用于当前可见的记忆条目 |
| **手动拉取** | 仅在技能面板中出现；进入当前智能体的页内扫描与拉取模式 |
| **批量删除** | 仅在技能面板且当前可见推送路径列表非空时出现，可进入多选模式 |

工具栏里的结果数也会跟随当前面板：
- 技能面板：只统计当前可见的推送路径卡片和扫描路径卡片。
- 记忆面板：只统计当前可见的记忆预览条目。
- 手动拉取模式：只统计当前可见的扫描结果卡片。

### 记忆面板

- 仅在顶部切换到 **记忆** 时显示。
- 直接读取当前选中智能体配置好的主记忆文件和规则目录，展示该智能体当前磁盘上的真实记忆内容。
- 分区头部提供 **刷新** 操作，可在不离开 **我的智能体** 的情况下重新读取该智能体当前落地的记忆状态。
- **记忆文件** 卡片展示配置路径、**打开文件** 操作以及当前文件内容预览；如果路径未配置，或已配置但文件暂时不存在，会保留路径区域并显示对应空状态，而不是让整个页面报错。
- **规则目录** 区域展示配置路径、**打开目录** 操作，以及该目录下每个平铺 `.md` 文件的预览卡片。
- 以 `sf-` 开头的规则文件会标记为 **托管**，方便区分 SkillFlow 推送的模块记忆和其他智能体本地规则文件。
- 顶部共享搜索框只会在 **记忆** 激活时过滤记忆预览；当搜索后没有匹配项时，当前面板会显示专门的空搜索提示，而不是复用技能分区的空状态。

### 技能面板

- 仅在顶部切换到技能时显示。

### 手动拉取模式

- 点击 **手动拉取** 不会打开单独页面或弹窗，而是把当前技能面板切换成当前智能体的页内拉取界面。
- 进入该模式时，会立即对当前选中智能体执行一次新的 `ScanAgentSkills(agentName)` 扫描。
- 目标分类直接在顶部工具栏中选择，不再使用独立的左侧分类栏。
- 手动拉取模式下，工具栏会显示：
  - 目标分类选择器
  - **全选 / 取消全选**
  - **选择未导入**
  - **开始拉取（n）**
  - **取消**
- 搜索与排序只作用于当前扫描结果。
- 扫描失败仍显示红色错误提示；扫描为空仍显示黄色警告提示。
- 拉取冲突仍复用共享的覆盖 / 跳过对话框，但整个处理流程现在发生在 **我的智能体** 页面内。
- 拉取成功后会自动退出该模式，并在技能面板顶部显示绿色完成提示。

### 推送路径分区

- 显示当前智能体推送目录中的可删除技能。
- 推送路径扫描会复用 **设置 → 通用** 中相同的深度限制（默认 `5`，保存范围 `1-20`）。
- 多选模式下，**全选 / 取消全选** 仅作用于当前可见、已过滤后的推送路径卡片。
- 如果智能体目录中的技能能关联到“我的技能”里已安装的记录，卡片会额外显示其来源徽标，区分手动导入和 Git 来源导入。
- 卡片继续显示“已导入 / 可更新 / 已推送到其他智能体”等真正有决策价值的状态；其中 `pushed` 使用紧凑的智能体图标列表，并会自动排除当前正在查看的智能体。

### 扫描路径分区

- 显示仅通过扫描目录发现的只读技能。
- 扫描路径发现会复用 **设置 → 通用** 中相同的深度限制（默认 `5`，保存范围 `1-20`）。
- 与推送路径共享同一组面板级搜索和排序控件。
- 扫描路径卡片也使用同一套紧凑状态条；当扫描项能关联到“我的技能”里已安装的记录时，还会显示来源 / 已导入 / 可更新 / 已推送到其他智能体等状态。“被扫描到”这件事则由分区本身表达，不再在每张卡片上重复显示 `detected` 徽章。

---

## 14. 我的提示词

用于在同步的 `prompts/` 目录中保存可复用的 system prompt。

### 导航与存储

- 左侧边栏在 **我的智能体** 下方新增 **我的提示词** 入口。
- 每条提示词都会在备份根目录下保存 `prompts/<category>/<name>/system.md`，并配套保存 sidecar 元数据文件 `prompts/<category>/<name>/prompt.json`，因此对象存储服务商与 Git 服务商都会自动同步同一批提示词文件。
- 提示词名称为必填项，在整个提示词库内全局唯一，并作为文件夹键名使用。

### 页面布局

- 页面改为和 **我的技能** 一致的左侧分类栏 + 右侧内容区结构。
- 内置 **Default** 作为默认分类。
- 与其他主路由一致，重新进入 **我的提示词** 页面时会重新挂载页面并拉取当前提示词数据，而不是长期保留旧页面实例。
- 提示词卡片会同时受到分类筛选、关键字搜索和 A-Z / Z-A 排序控制。
- 搜索支持逻辑 `and` / `or` 语法，例如 `review and golang` 或 `summary or changelog`。

### 分类管理

- 可在左侧边栏新建分类。
- 非默认分类支持右键重命名与删除。
- 提示词卡片支持拖拽到分类上移动，交互与 **我的技能** 的分类拖拽一致。

### 提示词卡片

- 卡片复用技能卡片的视觉语言（`card-base`、悬停发光、紧凑操作区）。
- 卡片默认展示 **名称**、可选 **描述** 和内容开头摘要。
- 提示词卡片列表中不再展示关联图片缩略图和网页链接胶囊，让列表更聚焦于文本摘要。
- 当内容超出预览范围时，会显示 **点击查看更多** 提示。
- 右上角按钮支持一键复制完整提示词内容到桌面剪贴板，在 Windows 上也会保留多行内容。
- 提示词复制现在会依次回退桌面运行时剪贴板、浏览器剪贴板和文档复制 API，因此某一条复制链路不可用时仍能完成复制。
- 右下角 **删除** 操作会先弹出确认对话框，再删除卡片和对应的提示词文件夹。

### 提示词编辑器

- 点击 **添加提示词** 会打开内置编辑器，包含 **名称**（必填）、**描述**（可选）、**分类** 和完整 `system.md` 内容。
- 编辑器支持填写最多 **3 个关联图片 URL**。已添加的图片会保留在图片区展示，而图片输入条会移动到正文之后的统一附件输入区，减少阅读干扰。
- 每张已添加图片的右上角都会提供删除操作。
- 点击编辑器里的图片缩略图，会在当前编辑器上方打开应用内放大预览层，而不是跳转到外部浏览器。
- 编辑器现在会限制弹窗正文和提示词正文输入区的高度，在窗口较小时改为内部滚动，避免底部确认按钮被挤出 SkillFlow 窗口。
- 编辑器支持通过放在正文之后统一附件输入区里的单行 markdown 输入框添加 **网页链接**。点击 **添加** 后，会把 `[产品文档](https://example.com/doc)` 中中括号里的文字作为可点击链接标签显示在上方，并清空当前输入框。
- 点击已有提示词卡片会打开同一编辑器，并载入现有内容以便编辑和重命名。
- 保存后会把正文写回 `prompts/<category>/<name>/system.md`，并把描述、图片 URL、网页链接等元数据写入 `prompts/<category>/<name>/prompt.json`。

### 导入 / 导出

- 工具栏 **导入** 支持读取 JSON 提示词库文件，并保留每条提示词在导入文件里的分类；如果本地已存在同名提示词，会在真正写入前让用户选择 **跳过** 还是 **覆盖**。
- 导入冲突对话框支持勾选 **对剩余 {count} 个冲突执行相同操作**，可把当前决策复用到本次导入剩余冲突，但不会保存成全局偏好。
- 工具栏 **导出** 不再点击后立刻落盘，而是先在页面内展开一条导出栏。
- 导出栏支持 **全部**、左侧当前选中分类，以及 **指定** 三种范围。
- **指定** 支持在左侧当前范围内多选提示词导出；左侧选中 **全部** 时可在全库多选，选中具体分类时只会在该分类内多选。
- 导出的 JSON 仍会保留每条提示词自己的分类、图片 URL 和网页链接。

---

## 15. 我的记忆

SkillFlow 提供统一的记忆管理工作台，支持在一处编写、组织和维护个人 AI 编程助手记忆，并把同一套记忆同步到多个智能体工具中。页面同时管理共享主记忆和可复用模块记忆，而「我的智能体」页可以直接验证实际同步结果。

### 记忆类型

- **主记忆**：单个 `main.md` 文件，包含跨所有已配置智能体共享的全局指令。
- **模块记忆**：按主题划分的独立 Markdown 文件（如 `coding-style`、`testing-rules`），存放于 `rules/` 目录下，并在推送时由主记忆显式引用。

### 页面布局

「我的记忆」页面展示：
- 顶部工具栏，包含 **搜索**、**新建模块**、**批量推送**。
- 带有各智能体推送状态标签的**主记忆卡片**。
- **双列模块网格**：每张模块卡片都显示内容摘要、全局启用/停用状态徽标、启停操作，以及模块引用提示。
- 顶部**自动同步面板**，可为每个已启用智能体设置 `关闭`、`自动合并`、`自动接管`。
- 内联式**批量推送**流程：进入选择态后，主记忆为必选项，模块卡片右上角出现复选框，顶部面板切换为目标智能体和本次统一推送模式设置。
- 固定在页面右侧的**编辑抽屉**，宽度约为视口的 72%，最大 960 px，只保留编辑、预览、保存、删除模块和在外部编辑器中打开。
- **新建模块对话框**：模块名需满足小写 `a-z`、`0-9`、`-` 规则，以便映射为智能体侧规则文件名。

### 批量推送流程

- 点击**批量推送**不会弹出模态框。
- 页面直接进入选择态：
  - 主记忆卡片保持选中且不可取消
  - 每个模块卡片右上角显示复选框
  - 顶部面板从**自动同步**切换为**选择推送目标智能体**
  - 用户可多选目标智能体
  - 用户只需为本次推送选择一次统一模式：**合并**或**接管**
- 真正执行时，SkillFlow 会把所选模块集合写入目标智能体，并按同一模块集合重建主记忆中的模块引用。
- 目标智能体规则目录中，凡是之前由 SkillFlow 管理、但这次未被选中的模块文件，都会在本次推送时被同步移除，使目标规则目录与当前选择保持一致。
- 模块引用会放在独立的 `<skillflow-module>...</skillflow-module>` 区块内，每行一个 Markdown 链接，例如 `[testing](rules/sf-testing.md)`。
- 这些模块链接会以主记忆文件为基准使用正斜杠相对路径，避免云同步时因不同主机路径而失效。
- 由于推送状态比较的是当前完整本地记忆集合，如果只做部分模块推送，该智能体仍会保持 **待推送**，直到它再次与当前本地主记忆和完整模块集合一致。

### 自动同步行为

- **自动同步**按智能体分别配置，提供 `关闭`、`自动合并`、`自动接管` 三种模式。
- 将某个智能体切换到自动同步模式时，SkillFlow 会立即把当前主记忆和全部当前已启用模块按所选模式推送到该智能体。
- 自动同步开启后，编辑主记忆、创建模块、保存模块、切换模块启用状态、删除模块，都会自动同步到所有开启自动同步的智能体。
- 停用某个模块会把它从自动同步基线中移除：SkillFlow 会同步删除各自动同步智能体规则目录中的对应 `sf-<name>.md` 托管文件，并从主记忆引用块中移除它。
- 当本地删除某个模块时，SkillFlow 不仅会更新主记忆中的模块引用，还会同步删除各自动同步智能体规则目录中的对应 `sf-<name>.md` 托管文件。

### 编辑行为

- 关闭编辑抽屉时，如果存在未保存修改，会提示用户选择**放弃修改**、**保存并关闭**或**继续编辑**。
- 预览选项卡会真正渲染 Markdown，而不是显示原始文本。
- 模块抽屉内的删除操作会先弹出独立确认对话框，展示模块名和一小段内容预览，再执行删除。

### 推送模式（按智能体）

| 模式 | 行为 |
|------|------|
| **合并** | SkillFlow 会把主记忆写在 `<skillflow-managed>...</skillflow-managed>` 区块内，再把模块引用写在独立的 `<skillflow-module>...</skillflow-module>` 区块内，区块外的其他内容仍然保留。写入前会先修复不完整的托管标签，确保下一次同步可以重建干净的托管区块。 |
| **接管** | SkillFlow 完整接管该智能体的记忆文件和规则目录中的托管记忆内容。 |

### 推送状态

每个智能体显示以下三种状态之一：

| 状态 | 含义 |
|------|------|
| ✓ 已同步 | 最近一次推送与当前本地主记忆和当前本地已启用模块集合一致。 |
| ⚠ 待推送 | 自上次完整推送以来本地记忆已发生变更，或该智能体当前只反映了部分模块选择。 |
| 从未推送 | 该智能体尚未收到过 SkillFlow 的记忆推送。 |

### 模块文件命名

模块记忆写入智能体规则目录时会添加 `sf-` 前缀（如 `coding-style` → `sf-coding-style.md`）。

### 外部编辑器

记忆抽屉提供「在编辑器中打开」按钮，可用系统默认文本编辑器打开文件。SkillFlow 会检测文件变更并自动刷新内容。

### 智能体设置

在**设置 → 智能体**中，每个智能体新增以下配置项：
- **Skill 路径**：分组展示推送路径和可多条编辑的扫描路径。
- **记忆路径**：分组展示主记忆文件和规则目录。
- **记忆文件**：智能体主记忆文件路径（显示默认值，用户可修改）。
- **规则目录**：智能体规则目录路径。对于没有官方规则目录概念的内置智能体（如 Copilot），该字段默认留空。

### 智能体侧校验

在 **我的智能体 → 记忆** 面板中，用户可以直接查看所选智能体当前磁盘上的主记忆文件和规则目录下的平铺 Markdown 文件，其中 `sf-*.md` 会被标记为 **托管**。这也是记忆自动同步和批量推送结果的直接校验界面。

### 云备份

记忆内容文件（`memory/main.md` 和 `memory/rules/*.md`）已纳入云备份范围。本地配置文件（`memory/memory_local.json`）不参与备份。

---

*最后更新：2026-04-17*
